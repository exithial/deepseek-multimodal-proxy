# Release v1.7.0 - DeepSeek Multimodal Proxy

🚀 **Nueva versión lista para producción**

## 📊 Resumen de Cambios

Esta versión corrige un problema crítico con el modelo de razonamiento y añade herramientas de calidad de código.

### 🐛 Bug Fixes

- **Error en modelo reasoner**: Corregido error 400 de DeepSeek API que ocurría al mantener conversaciones largas. Ahora el campo `reasoning_content` se gestiona correctamente.
- **Compatibilidad Anthropic**: Mapeo bidireccional de bloques `thinking` ↔ `reasoning_content`.

### ✨ Nuevas Características

- **ESLint integrado**: Verificación automática de código TypeScript.
  ```bash
  npm run lint      # Verificar código
  npm run lint:fix # Corregir automáticamente
  ```

### ⚙️ Mejoras

- Lockfiles simplificado: solo `yarn.lock` en el proyecto.
- Código más limpio: eliminados warnings y variables no usadas.

---

**deepseek-multimodal-proxy** - Proxy OpenAI-compatible con capacidades multimodales
